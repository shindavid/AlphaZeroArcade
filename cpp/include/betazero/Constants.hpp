#pragma once

namespace beta0 {

constexpr float kShockZScore = 2.0f;  // shock scores track 2-sigma possibilities
constexpr float kShockMult = 1.01f;   // 1% increase per shock step

}  // namespace beta0
