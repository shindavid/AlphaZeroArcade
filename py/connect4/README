Generating training data:
mpirun -n 6 ./generate_training_data.py -c=~/connect4/ -n 10000

Training a model:
./train_ny.py 


Installing Node/React dependencies for the log replay tool:

conda install nodejs

Generating a play log:

python3 play_vs_cpu.py -d first_run.xml

Launching the node server:

npm start

